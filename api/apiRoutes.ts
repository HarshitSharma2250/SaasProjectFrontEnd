import { apiRoutes } from "@/types/apiRoutes.types";

export const APIsRoutes:apiRoutes={
    LOGIN:'/api/login',
    REGISTER:'/api/register',
    DASHBOARD_MATRIX:'api/keyMatrics',
    USER_REVINUE_DATA:'api/userData'
}